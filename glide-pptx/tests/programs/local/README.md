Drop a talk here -- its `.rhm` and everything beside it that it needs -- and
`action-fuzz.rkt` will deal random combinations of edits at it along with the
fixtures, and `editable.rkt` will report what can be edited in it.

Nothing here is tracked. A talk is not ours to commit, and it is the one program
whose editability anybody is going to complain about.
